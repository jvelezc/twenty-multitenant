# SleepNest CRM - Environment Configuration
# ==========================================
# Copy this file to .env and fill in your values
#
# cp .env.example .env
# nano .env

# ==================== ADMIN USERS ====================
# IMPORTANT: These email addresses will have full admin access
# They can manage all tenants, view cross-tenant data, disable accounts, etc.
# Comma-separated list (no spaces after commas)
ADMIN_EMAILS=admin@yourcompany.com,ceo@yourcompany.com

# ==================== SUPABASE AUTH ====================
# Get these from your Supabase project dashboard:
# https://supabase.com/dashboard/project/_/settings/api

# Your Supabase project URL (e.g., https://xxxxx.supabase.co)
SUPABASE_URL=https://your-project.supabase.co

# Public anon key (safe to expose in frontend)
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Service role key (KEEP SECRET - server-side only)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# JWT secret (Settings > API > JWT Settings > JWT Secret)
SUPABASE_JWT_SECRET=your-jwt-secret-from-supabase

# ==================== SERVER CONFIG ====================
# Your server's public URL (no trailing slash)
SERVER_URL=https://crm.yourcompany.com

# Random secret for signing tokens (generate with: openssl rand -hex 32)
APP_SECRET=replace_me_with_a_random_string_at_least_32_chars

# ==================== DATABASE ====================
# Only change if using external database
PG_DATABASE_USER=postgres
PG_DATABASE_PASSWORD=postgres
PG_DATABASE_HOST=db
PG_DATABASE_PORT=5432
PG_DATABASE_NAME=default

# ==================== OPTIONAL: S3 STORAGE ====================
# Uncomment and configure for S3-compatible storage (DigitalOcean Spaces, AWS S3, etc.)
# STORAGE_TYPE=s3
# STORAGE_S3_REGION=nyc3
# STORAGE_S3_NAME=your-bucket-name
# STORAGE_S3_ENDPOINT=https://nyc3.digitaloceanspaces.com

# ==================== WEBHOOK SECRET ====================
# Secret for verifying webhook signatures from external systems (billing, etc.)
# External systems sign their requests with this secret, and we verify them.
# Generate with: openssl rand -hex 32
WEBHOOK_SECRET=your-webhook-secret-here

# ==================== SAAS ADMIN KEY ====================
# Master API key for platform operators (you).
# Provides FULL access to all tenant management operations.
# Use this for: automation scripts, admin dashboards, billing integrations.
# Generate with: openssl rand -hex 32
# Header: x-saas-admin-key
SAAS_ADMIN_KEY=your-saas-admin-key-here

# ==================== OPTIONAL: EMAIL ====================
# Uncomment to enable email notifications
# EMAIL_FROM_ADDRESS=noreply@yourcompany.com
# EMAIL_FROM_NAME="SleepNest CRM"
# EMAIL_SYSTEM_ADDRESS=system@yourcompany.com
# EMAIL_DRIVER=smtp
# EMAIL_SMTP_HOST=smtp.gmail.com
# EMAIL_SMTP_PORT=465
# EMAIL_SMTP_USER=your-email@gmail.com
# EMAIL_SMTP_PASSWORD=your-app-password
