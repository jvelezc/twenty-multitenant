/**
 * Subscription tier enum - kept for compatibility but all users get FULL access.
 * This CRM does not have tiered pricing - everyone enjoys 100% of the product.
 */
export enum SubscriptionTier {
  FULL = 'full', // Everyone gets this - full access to all features
}
